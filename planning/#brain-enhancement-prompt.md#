
Task: Make a targeted refactor to enhane the info presentation on the global brain    │
│   panel. This top panel is a cool little thing display the autocomplete suggestions in  │
│   a discovery kinda format. The inspiration here is StumbleUpon / an app I found        │
│   called Crapboard. The main refactoring that is needed here is some kind of history    │
│   for representing the cards. Whenever you click next, you go to the next card          │
│   randomly, and that's all good and stuff, but, um, well, first of all, is next         │
│   actually random or is it doing it in order? It seems like it could be improved. Is    │
│   this even pulling from the correct JSON, or is this pulling from a hard-coded         │
│   subset? I think it's actually just a hard-coded subset now that I'm playing with it,  │
│   and so we need to make this actually pull from the real JSON data. That wasn't even   │
│   what I was expecting to have to look at here. I didn't realize it was still using     │
│   hardcoded data, so we will need to make it use the Global Brain JSON, like how the    │
│   actual suggestion system uses. Anyways, I wanted to implement a kind of history on    │
│   it, so just let there be kind of a back button. Whenever you click the back button,   │
│   then you can also click forward. So whenever you click next and you kind of stumble   │
│   through the options, there should be a way to go back and forward. Currently there's  │
│   no history with it. And the history should dynamically generate in memory as the      │
│   user clicks the next button. When the user clicks next from historical item, it       │
│   should 